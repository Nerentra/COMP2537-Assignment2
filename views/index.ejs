<%- include("templates/header", { title: "Home", user }) %>
  <% if (user && user.name) { %>
    <p class="mt-3">Hello, <%- user.name %>!</p>
    <a href="/members" class="btn btn-primary">Go to Members Area</a><br />
    <a href="/logout" class="btn btn-secondary mt-2">Logout</a><br />
  <% } else { %>  
    <a href="/signup" class="btn btn-success">Sign up</a><br />
    <a href="/login" class="btn btn-primary mt-2">Log in</a>
  <% } %>
  </div>
<%- include("templates/footer") %>
